# Bootstrap: zones, adjacency, base exposure (manual, once).
# Implementation agent: load OCHA COD-AB geometries for Mandera cluster.

print(
    "[bootstrap] Scaffold only — seed clusters, zones, zone_adjacency, zone_exposure."
)
raise SystemExit(1)
