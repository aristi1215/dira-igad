[project]
name = "dira-core"
version = "0.1.0"
description = "Pure domain: situations, risk bands, alert invariants, and ports. No I/O."
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["dira_core"]
